import 'package:flutter/material.dart';

class Erro404Page extends StatelessWidget {
  const Erro404Page({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello ERROR Page'),
      ),
    );
  }
}
